import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DatePlanPage } from './date-plan';

@NgModule({
  declarations: [
    // DatePlanPage,
  ],
  imports: [
    // IonicPageModule.forChild(DatePlanPage),
  ],
})
export class DatePlanPageModule {}
