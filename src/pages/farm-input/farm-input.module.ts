import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { FarmInputPage } from './farm-input';

@NgModule({
  declarations: [
    // FarmInputPage,
  ],
  imports: [
    // IonicPageModule.forChild(FarmInputPage),
  ],
})
export class FarmInputPageModule {}
